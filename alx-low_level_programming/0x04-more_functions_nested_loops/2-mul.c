#include "main.h"
/**
 * mul - Entry point, multipies two digits
 * @a: user input
 * @b: user input
 * Return: result
 */
int mul(int a, int b)
{
	int result = 0;

	result = a * b;

	return (result);
}
