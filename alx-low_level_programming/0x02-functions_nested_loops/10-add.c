#include "main.h"
/**
 * add - Entry point, adds two integers together
 * @a: user input
 * @b: user input
 * Return: result of addition
 */
int add(int a, int b)
{
	int result = 0;

	result = a + b;

	return (result);
}
